! For the sake of future readers of this file (yourself included),
! ONLY include the controls you are actually using.  DO NOT include
! all of the other controls that simply have their default values.

&star_job

! save a model at the end of the run
  	load_saved_model = .true.
	load_model_filename = 'models_to_load/2_post_burst_models/hot_accretion_test_1d-5.mod'

! set initial composition and nuclear reaction network
  	set_uniform_initial_composition = .false.
  	initial_zfracs = 8 

! hydro 
	change_v_flag = .true.
	new_v_flag = .true. !.false.

! initial conditions
    set_initial_dt = .true.
	years_for_initial_dt = 1d2 ! 100 years

	show_retry_counts_when_terminate = .true.
	show_timestep_limit_counts_when_terminate = .true.

	set_initial_model_number = .true.
	set_initial_cumulative_energy_error = .true.

! Save model when terminate
	save_model_when_terminate = .false. ! can set to false if one just wants a constant accretion model
	! save_model_filename = 'models_to_load/pre_burst_Z0.02_amlt2.0_overshoot_ledoux'

/ !end of star_job namelist

&controls
! LOGS and profiles
	star_history_name = 'history_post_burst.data'
	profiles_index_name = 'profiles_post_burst.index'
	profile_data_prefix = 'profile_post_burst'

! initial model flag
	x_logical_ctrl(2) = .false.

! Whether or not we set initial composition (we only do this when we evolve a constant accretion model or the pre-burst model)
	x_logical_ctrl(5) = .false.

! integer control for whether to save a pre-burst model or post-burst model
	x_integer_ctrl(10) = 2 ! 0 = save pre-burst model 100 years before burst,
						  ! 1 = save post-burst model 1000 years after burst
						  ! 2 = use h1 termination string

! Varcontrol; limits how much structure can change in a step
    varcontrol_target = 1d-4
	
/ ! end of controls namelist
